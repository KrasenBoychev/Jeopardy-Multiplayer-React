// Exit Game:

1. + Test if friendsList will update when click Exit Game
2. + Check if the players successfully leave the room
3. + Game in progress should be sent to both players
4. + Set notifications to update
5. + Send update to friends that the user is online
6. + Leave a room if there was another one before
7. + Check if friends status will change after click Exit Game if they log in or our during the game

// Components that need to be deleted + their css files:
    + JoinGame.jsx
    + ConnectPlayers.jsx

// Hooks and other files that need to be deleted:
    + usePlay.js
    + useLeaveGame.js
    - GameContext.jsx

// Uninstall the following:
    - stream chat library
    - react-spinners


// When game finishes:

1. When game finishes, get all notifications from the server - this will trigger the notifications 
    received during the game and avoid showing any game invitations if any left by mistake


// Last step:
 - Check all files in "game" folder for commented code!