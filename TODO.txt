- Complete the Create Components
   - question details should show when "Previous Page" is clicked
   - category name should show when "Previous Page" is clicked
   - formValues set to empty strings byy default - they don't update. 
         Try to give a certain value to CreateQuestion from the parent component.
- Check for all folders what needs to be deleted
--------------------------------------------------------------------------------------

1. Redux management system:
   - better error handling for the game components
   - better error handling when fetching or posting data
   - create a Public Guard for Login and Register (after backend is completed);

2. Based on the UI, rework CreateQuestion.jsx - it needs to be devided to simplier components, 
   but the logic has to be reworked as well

3. Fix the backend - keep the user logged in when the page is refreshed