Sockets:
 - When a user disconnects, check if the user has another active token - in case the user is logged in a few different tabs
    - If the same user logs in from a different browser, the name appears twice or more times to the rest of the users in the "Pick opponent" box

- Remove the external .css files and replace them with tailwind css

Final Step:
    - Delete all unused components and funcs on the client and the server
    - Check if everything works again